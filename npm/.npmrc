# NPM Configuration

# Use exact versions by default (no ^ or ~)
save-exact=true

# Default registry
registry=https://registry.npmjs.org/

# Reduce npm output noise
loglevel=warn

# Fund messages are annoying
fund=false

# Audit on install (security)
audit=true
